// Variáveis e tipos de dados
// Declaração or declaration

var name

// Assigment or atribuição de valores

name = "Mike"

// que tipo de dado foi colocado na variavél

console.log( typeof name);

// Agrupar as váriaveis

let age, isHuman

age = 18
isHuman = true

// Multiplos argumentos na função

console.log(name, age, isHuman);

// Escrita de texto + váriaveis / Concatenando valores.

console.log("o "+ name + " tem " + age + " anos")

// Interpolando valores com template literals, template strings.

console.log(`o ${name} tem ${age} anos.`)